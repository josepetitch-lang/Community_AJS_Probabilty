function initUI(): void {
    console.log("Sistema de Ajedrez: Interfaz inicializada correctamente.");
    document.body.classList.add('loaded');
}
document.addEventListener('DOMContentLoaded', initUI);